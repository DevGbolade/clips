import { ClickEventEmitterDirective } from './click-event-emitter.directive';

describe('ClickEventEmitterDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickEventEmitterDirective();
    expect(directive).toBeTruthy();
  });
});
